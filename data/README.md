# data/

No file-based model data lives here. Supabase is the single source of truth.

Test fixtures live in `tests/fixtures/` with dated filenames:
- `ai_models_snapshot_2025-11-30.json` — frozen 173-model snapshot, epoch-int `created` format (pre-migration artifact)

The `schemas/` subdirectory contains database schema documentation.
